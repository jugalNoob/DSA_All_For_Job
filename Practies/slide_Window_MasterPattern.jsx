⚡ MASTER TEMPLATE (Use Everywhere)
for (right = 0 → n) {

    // Expand
    include current element

    // Handle condition (duplicate / invalid)
    while (invalid condition) {
        remove from left
        left++
    }

    // Update answer
}