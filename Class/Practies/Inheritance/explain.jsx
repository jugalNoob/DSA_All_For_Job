| Concept       | Meaning                                            | JS Example                                                  |
| ------------- | -------------------------------------------------- | ----------------------------------------------------------- |
| Encapsulation | Hide internal data, access via methods             | `#password` with `login()`                                  |
| Abstraction   | Hide implementation details, expose only essential | `Vehicle.startEngine()` throws in base                      |
| Interfaces    | Contract: class must implement methods             | `Payment` base class + `CreditCardPayment`, `PayPalPayment` |
